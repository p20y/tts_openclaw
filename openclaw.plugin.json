{
  "id": "kokoro-local-tts-plugin",
  "name": "Kokoro Local TTS Plugin",
  "description": "Local Kokoro TTS for OpenClaw with Apple M4 (MPS) acceleration and CPU fallback",
  "version": "1.0.2",
  "main": "./index.js",
  "configSchema": {
    "type": "object",
    "properties": {
      "pythonPath": {
        "type": "string",
        "description": "Optional python executable path for Kokoro runtime"
      }
    }
  }
}
